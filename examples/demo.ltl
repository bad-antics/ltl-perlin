// examples/demo.ltl — runnable demo for ltl_perlin

import std::io
use ltl_perlin as lib

fn main() {
    io::println("ltl_perlin demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}